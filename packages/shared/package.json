{
  "name": "@ashirwad-shetye/agentlayer-shared",
  "version": "0.1.3",
  "type": "module",
  "files": [
    "README.md",
    "dist"
  ],
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\" && tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
